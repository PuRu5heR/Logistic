public class GroundTransport extends Transport {
    public GroundTransport(double loadCapacity, double transportationPrice, double speed, double passengers) {
        super(loadCapacity, transportationPrice, speed, passengers);
    }
}
