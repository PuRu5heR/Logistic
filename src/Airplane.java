public class Airplane extends AirTransport {
    public Airplane(double loadCapacity, double transportationPrice, double speed, double passengers) {
        super(loadCapacity, transportationPrice, speed, passengers);
    }
}
