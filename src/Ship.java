public class Ship extends WaterTransport {
    public Ship(double loadCapacity, double transportationPrice, double speed, double passengers) {
        super(loadCapacity, transportationPrice, speed, passengers);
    }
}
