public class WaterTransport extends Transport {

    public WaterTransport(double loadCapacity, double transportationPrice, double speed, double passengers) {
        super(loadCapacity, transportationPrice, speed, passengers);
    }
}
