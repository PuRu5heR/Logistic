public class AirTransport extends Transport {
    public AirTransport(double loadCapacity, double transportationPrice, double speed, double passengers) {
        super(loadCapacity, transportationPrice, speed, passengers);
    }
}
